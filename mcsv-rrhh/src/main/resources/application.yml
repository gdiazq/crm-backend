server:
    port: 9096

spring:
    application:
        name: mcsv-rrhh

    config:
        import: "configserver:"

    servlet:
        multipart:
            enabled: true
            max-file-size: 10MB
            max-request-size: 55MB

    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8888}

info:
  app:
    name: rrhh Microservicio Server
    description: Microservicio para manejar recursos humanos
    version: 1.0.0

# Configuración para Documentation swagger
openapi:
    service:
        title: rrhh-service doc
        version: 1.0.0
        url: ${URLAPI:http://localhost:9090}/v1/api/rrhh

feign:
  circuitbreaker:
    enabled: true

resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
    instances:
      mcsv-user:
        baseConfig: default
  timelimiter:
    configs:
      default:
        timeoutDuration: 3s
    instances:
      mcsv-user:
        baseConfig: default

aws:
    s3:
        enabled: true
        bucket: ${AWS_S3_BUCKET:crm-uploads}
        region: ${AWS_REGION:us-east-1}
        access-key: ${AWS_ACCESS_KEY:}
        secret-key: ${AWS_SECRET_KEY:}

springdoc:
  api-docs:
    path: /api/api-docs/rrhh
  swagger-ui:
    path: /api-docs/swagger-ui.html
